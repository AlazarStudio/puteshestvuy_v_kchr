# Путешествуй в КЧР

Современный проект на **React + Vite** для коммерческого сайта о путешествиях в Карачаево-Черкесию.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Сборка для продакшена

```bash
npm run build
npm run preview   # просмотр собранного приложения
```

Собранные файлы будут в папке `dist/`.

## 📂 Структура проекта

```
src/
 ├── App.jsx               # Роутинг и корневой компонент
 ├── main.jsx              # Точка входа
 ├── index.css             # Глобальные стили (Tailwind)
 │
 ├── app/                  # Страницы и админка (остаток от Next.js)
 │   ├── not-found.jsx     # Страница 404
 │   └── admin/            # Админ-панель
 │
 ├── components/           # Переиспользуемые компоненты
 │   ├── ui/
 │   └── layout/           # Header, Footer, LayoutWrapper
 │
 ├── sections/             # Секции страниц (Main, Region, Routes, Places, News, Services)
 ├── lib/                  # API и утилиты
 └── utils/
```

## 🎨 Стилизация

Проект использует **Tailwind CSS** для стилизации:

- `src/index.css` — глобальные стили и утилиты Tailwind
- `tailwind.config.js` — конфигурация Tailwind (цвета, шрифты, контейнеры)

### Цветовая палитра

Основной цвет: `primary` (оттенки синего)
- Используйте классы: `bg-primary-600`, `text-primary-600` и т.д.

### Контейнеры

Используйте класс `.container-content` для центрированного контента с отступами:

```jsx
<div className="container-content">
  {/* Ваш контент */}
</div>
```

## 🧩 Добавление новых страниц

Добавьте маршрут в `src/App.jsx` (react-router-dom) и создайте компонент страницы в `src/sections/` или `src/app/`.

## 🧩 Добавление компонентов

### UI компоненты

Создавайте в `src/components/ui/`:

```jsx
// src/components/ui/YourComponent.jsx
export default function YourComponent({ children, ...props }) {
  return (
    <div {...props}>
      {children}
    </div>
  )
}
```

### Layout компоненты

Создавайте в `src/components/layout/` для компонентов, которые используются в макете (Header, Footer, Sidebar и т.д.)

## 📦 Секции

Секции — это крупные блоки контента, которые используются на страницах. Создавайте их в `src/sections/`:

```jsx
// src/sections/YourSection.jsx
import { motion } from 'framer-motion'

export default function YourSection() {
  return (
    <section id="your-section" className="py-20">
      <div className="container-content">
        {/* Ваш контент */}
      </div>
    </section>
  )
}
```

## 🎭 Анимации

Проект использует **Framer Motion** для анимаций. Пример использования:

```jsx
import { motion } from 'framer-motion'

<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.6 }}
>
  {/* Контент */}
</motion.div>
```

## 🔗 Алиасы путей

Проект настроен на использование алиасов `@/`:

- `@/components` → `src/components`
- `@/sections` → `src/sections`
- `@/hooks` → `src/hooks`
- `@/lib` → `src/lib`
- `@/styles` → `src/styles`

Пример:

```jsx
import Button from '@/components/ui/Button'
import Hero from '@/sections/Hero'
```

## 🛠 Технологии

- **Vite 6** — сборщик
- **React 18**
- **React Router 6** — маршрутизация
- **Tailwind CSS 3**
- **Framer Motion** (анимации)
- **JavaScript** (без TypeScript)

## 📄 Лицензия

Проект готов для коммерческого использования.
