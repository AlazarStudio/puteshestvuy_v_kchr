# Переменные, которые использует ТОЛЬКО `npm run build` (перекрывают .env).
# Пока боевой API-домен не создан, прод собирается на devbackend — как и работал.
# Когда появится боевой контур API (см. docs/deploy/api-domain.md),
# заменить одну строку ниже и пересобрать.
VITE_API_URL="https://devbackend.xn--b1aahavh3aoehdhg3dg.xn--p1ai/api"
