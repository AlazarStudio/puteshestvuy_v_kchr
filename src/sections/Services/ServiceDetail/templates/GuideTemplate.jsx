import ServiceDetail from '../ServiceDetail'

/** Шаблон страницы гида — полный layout с галереей, отзывами и т.д. */
export default function GuideTemplate() {
  return <ServiceDetail serviceSlug={null} />
}
