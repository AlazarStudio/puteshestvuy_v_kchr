import ActivityDetail from '../ActivityDetail'

/** Шаблон страницы активности — полный layout: галерея, программа, пакеты, экипировка, безопасность, отзывы */
export default function ActivitiesTemplate() {
  return <ActivityDetail serviceSlug={null} />
}
